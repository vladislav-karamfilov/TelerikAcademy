var Animation = Class.create({
    initialize: function (url, width, numberOfFrames, currentFrame, rate) {
        this.url = url;
        this.width = width;
        this.numberOfFrames = numberOfFrames;
        this.currentFrame = currentFrame;
        this.rate = rate;
    }
});