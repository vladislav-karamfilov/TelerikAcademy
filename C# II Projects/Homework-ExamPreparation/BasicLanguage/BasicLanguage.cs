using System;
using System.Text;

class BasicLanguage
{
    static void Main()
    {
        StringBuilder input = new StringBuilder();
        string inputLine = null;
        do
        {
            inputLine = Console.ReadLine();
            input.Append(inputLine);
        } while (inputLine != "EXIT;");
        string[] constructions = GetConstruction
    }
}
